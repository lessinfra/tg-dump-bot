{
  "private": true,
  "name": "json-dumper-tg-bot",
  "version": "1.0.0",
  "description": "A simple telegram bot to dump JSON",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "format": "prettier --write '**/*.{js,css,json,md}'"
  },
  "author": "kcubeterm <kcubeterm@gmail.com>",
  "license": "MIT",
  "devDependencies": {
    "prettier": "^1.18.2"
  }
}
